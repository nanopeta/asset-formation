# 資産形成ダッシュボード TODO

## ファイル構成
```
asset-formation/
├── index.html   (~1,360行) — マークアップのみ
├── style.css    (~590行) — スタイル
├── app.js       (~1,990行) — ロジック
├── sw.js        — Service Worker（PWA・通知）
├── manifest.json — PWA マニフェスト
├── CLAUDE.md    — AI向けコードベース概要
├── README.md    — 機能概要
└── TODO.md      — このファイル
```

---

## 完了済み ✅

### 基盤
- [x] **[完了 v106] モバイル入力欄の文字サイズ最適化** — `@768px` の入力欄/プルダウンを 16→14px（Android で過大に見える問題を解消）。iOS のみ `@supports(-webkit-touch-callout:none)` で 16px 維持しズーム防止
- [x] **[完了 v105] タイポグラフィ（文字高）最適化** — `body` に基本行間（`--lh-base:1.5`）を設定、極小文字（9〜10px のボトムナビ・配当カレンダー・並び替えボタン等）を +1px。テーブル/バッジ/KPI 寸法は据え置き
- [x] HTML / CSS / JS を3ファイルに分割
- [x] localStorage によるデータ永続化（v3形式）
- [x] 旧フォーマット（v1/v2）からの自動マイグレーション
- [x] キャッシュバスター（?v=N でバージョン管理）
- [x] レスポンシブレイアウト（768px / 480px / 900px ブレークポイント）

### ダッシュボード
- [x] 総資産ヒーローカード（グラデーション背景、含み損益・先月比）
- [x] 投資資産 / iDeCo / 現金 の3枚サマリーカード
- [x] SCHD 元本進捗コンパクトストリップ（到達予想付き）
- [x] 楽天SCHD スポット買いおすすめ表示（決算日ベース・NISA/特定口座自動判定）
- [x] NISA 枠カード（成長投資枠・積立投資枠・生涯枠・成長枠生涯上限）
- [x] 今年の投資計画（成長投資枠・積立投資枠・iDeCo の年間計画行）
- [x] スポット購入計画（銘柄ごとに複数件、完了チェック付き）
- [x] ポートフォリオ ドーナツチャート + 銘柄一覧（フルワイドカード）
- [x] 目標配分バー（種別ごとの実績 vs 目標）+ 買い増し目安金額
- [x] クイックナビ（ダッシュボード内スムーズスクロール・スクロール連動ハイライト・シミュレーション系ドロップダウン統合）
- [x] 先月スナップ未記録リマインダー（黄色バナー、記録タブへ直接遷移）

### 分析セクション
- [x] 資産推移折れ線チャート（期間フィルター: 全期間/1年/6ヶ月/3ヶ月）
- [x] SCHD 到達シミュレーション（対象銘柄を設定から選択可能）
- [x] 分配金再投資シミュレーション（年数・月次積立・配当成長率・年次リターン・再投資なし・目標月収ハイライト・折れ線チャート）
- [x] 配当カレンダー（月別受取スケジュール・棒グラフ）
- [x] 全銘柄配当シミュレーション（NISA非課税 / 特定口座課税 / iDeCo非課税）
- [x] iDeCo 受取シミュレーション（一時金・年金方式、退職所得控除・概算税額計算）
- [x] FIRE 達成シミュレーション（必要資産・達成年数・プログレスバー）
- [x] FIRE 取崩しシミュレーション（資産枯渇年計算・折れ線チャート・年次内訳テーブル）
- [x] 税金概算（配当税・潜在税・NISA/iDeCo非課税メリット・特定口座銘柄別内訳）
- [x] 全銘柄詳細分析
  - [x] 口座別合計ドーナツグラフ + テーブル
  - [x] 種別合計ドーナツグラフ + テーブル
  - [x] 銘柄別一覧（縦横スクロール、ヘッダー・A列固定、Excel風フィルター）

### 記録タブ
- [x] Sticky 記録月 + 保存ボタン
- [x] 未保存インジケーター（パルスアニメーション）
- [x] 過去月セレクト・データ読み込み・編集・削除
- [x] スナップショット一覧テーブル
- [x] 保存後の月次サマリーモーダル（総資産・先月比・含み損益）
- [x] iDeCo 累計拠出元本の自動推計（開始月×月次拠出）

### 設定タブ
- [x] 保有銘柄 CRUD（口座・種別・証券会社・月次・スポット購入計画・配当利回り・ドラッグ並び替え）
- [x] iDeCo 銘柄 CRUD
- [x] 銀行口座 CRUD
- [x] クレジットカード CRUD（引き落とし口座紐付け）
- [x] 証券会社 CRUD（銘柄への紐付け・楽天証券CSVインポートの削除スコープ制御）
- [x] 口座種別 CRUD（組み込み編集・非課税フラグ・カスタム追加・ドラッグ並び替え・カラーピッカー＋ランダム色）
- [x] 銘柄種別 CRUD（組み込み編集・カスタム追加・ドラッグ並び替え・カラーピッカー＋ランダム色）
- [x] 対象銘柄セレクト・USD/JPY 設定
- [x] iDeCo 開始月・月次拠出合計設定
- [x] 全体バックアップ JSON エクスポート / インポート
- [x] 設定のみ JSON エクスポート / インポート
- [x] 記録 CSV エクスポート
- [x] AI分析用エクスポート（ChatGPT・Claude 向け Markdown .md）
- [x] 基本設定に資産形成方針メモ欄を追加（AI分析用エクスポートに反映）
- [x] 楽天証券 CSV インポート（Shift-JIS / UTF-8 対応、確認ダイアログ付き、証券会社スコープで削除対象を制御）
- [x] 楽天証券 CSV インポート — 楽天銀行残高（楽天銀行普通預金残高）も自動取込み
- [x] 楽天証券 CSV インポート — 預り金（JPY現金残高）も自動取込み
- [x] 楽天証券 CSV インポート — 保有商品詳細の全行を取込み（未知の種別・口座種別はインポート時に自動新規作成）

### UI/UX
- [x] Excel 風フィルター（昇降順ソート・値チェックフィルター、複数テーブル対応）
- [x] フィルター適用時に合計行を可視行のみで再計算
- [x] テーブル全般：タイトル固定・A列固定・ヘッダー固定・横スクロール対応
- [x] Toast 通知（成功・エラー・`role="alert"` アクセシビリティ属性）— 5秒タイムアウト・× 閉じるボタン付き
- [x] ヘルプ・更新履歴モーダル（バージョンバッジクリックで開く、使い方タブ＋更新履歴タブ）
- [x] h1 タイトルクリック時の未保存確認ダイアログ（`handleTitleClick()`）
- [x] `TAX_RATE` 定数化（税率 20.315% を先頭で一元定義し、コード内の直書き数値を全廃）
- [x] `formatMonth(d)` ユーティリティ（月 YYYY-MM フォーマットの重複コードを集約）
- [x] `FileReader.onerror` ハンドラ追加（`importAll` / `importSettings`）
- [x] ドラッグ&ドロップ並び替え（銘柄・口座・種別・証券会社）
- [x] iOS 対応エクスポート（Web Share API 優先）
- [x] 設定パネルのモーダルポップアップ化（背景クリックで閉じる・`_panelOpen`/`_panelClose` で一元管理）
- [x] ESCキーでモーダル・パネル・確認ダイアログを閉じる
- [x] スポット完了行の視覚フィードバック（取り消し線・opacity低下）
- [x] 姉妹アプリ「給与明細ダッシュボード」とのUI規約統一（カードタイトル左アクセントバー・チャートY軸「万」表記・タブ切替時のスクロールリセット）
- [x] レイアウトのSidebar/BottomNav化（姉妹アプリと統一。PC=左固定サイドバー、スマホ=モバイルヘッダー＋ボトムナビ。姉妹アプリへの外部リンクも追加）
- [x] iDeCo 色を全箇所（バッジ・ドーナツ・トレンドチャート）で統一（#c9915a・`b-ideco` クラス）
- [x] 口座種別/銘柄種別カラーピッカーをグラデーション（色相）順に整列
- [x] ランダム色ボタン（他種別で使用中の色を除外した未使用色からランダム選択）
- [x] 入力欄 min="0" バリデーション（銀行・カード・銘柄・スポット）
- [x] スポット削除ボタンに aria-label アクセシビリティ属性
- [x] iOS ズーム防止（モバイル幅で入力欄の font-size を 16px に）
- [x] ヘッダーにバージョンバッジ表示（`APP_VERSION` 定数で管理、キャッシュバスターと同期）
- [x] ブランデッド確認ダイアログ（`customConfirm`）— ブラウザ標準 `confirm()` を全廃
  - 削除操作（銘柄・銀行・カード・証券会社・口座種別・銘柄種別・スナップ）すべてに適用
  - 楽天証券 CSV インポート確認にも適用（HTML エスケープ済み）
  - 未保存変更ありタブ切替・スナップ読み込み時にも適用
- [x] NISA 枠超過を視覚的に表示（`fill-red` バー + 「超過 ¥xxx」テキスト + 赤文字）
- [x] 全体バックアップ インポート時のバリデーション（`settings` / `holdings` 必須フィールドチェック）
- [x] 目標配分合計が 100% 超の場合に Toast エラー通知
- [x] Chart.js インプレース更新最適化（`_chartRender` ヘルパー）
  - ラベル数・データセット数が同じ場合は `chart.update('active')` で再描画（DOM 操作ゼロ）
  - 適用チャート: ポートフォリオ / 口座別 / 種別別 / 配当カレンダー / 再投資シミュ / 資産推移
  - 配当カレンダーは `<canvas>` 要素の永続化も合わせて対応

### E: 機能追加（v76〜v83）

- [x] 月末リマインダー通知（PWA / Android Chrome Periodic Background Sync）— `requestNotifPermission()` / `_registerPeriodicSync()` / `_renderNotifStatus()`
- [x] 月末リマインダーは記録済み判定を廃止（毎月末に通知、`_registerPeriodicSync()` で管理）
- [x] ヘッダーに今日の日付（曜日）を表示（`updateTodayDate()`・`#today-date`）
- [x] スマホヘッダーレイアウト修正（480px以下で最終更新非表示・h1縮小）
- [x] 種別合計ドーナツに「現金」を追加（銀行残高を種別ポートフォリオに含める）
- [x] 現金の色を設定タブで変更可能に（設定保存・ドーナツ即反映）
- [x] 凡例クリックでテーブル行連動（種別合計ドーナツ凡例 → 詳細テーブル行ハイライト）
- [x] 資産分析レポート機能（`renderAssetReport()` / `openAssetReport()` / `closeAssetReport()`）
  - 口座別・銘柄種別バー比較（実額・比率付き）
  - インサイト自動診断（12条件: 集中リスク・NISA活用状況・iDeCo進捗・配当バランス等）
- [x] 分析レポート NISA 成長枠計算バグ修正（v83）

### D: 大規模機能（v67〜v75）

- [x] ヘルプ・更新履歴モーダル（バージョンバッジクリックで開く、使い方タブ＋更新履歴タブ）— v75
- [x] ダークモード（🌙ボタンでテーマ切り替え、localStorage に保存）— v68/v69
- [x] スナップショット比較モーダル（過去月との差分をモーダル表示）— v73
- [x] PWA 化 — manifest.json + Service Worker (sw.js) + アイコン SVG (192/512px) + `<head>` 登録
- [x] ポイント口座管理
  - `D.pointAccounts[]` + `D.current.pointValues{}` データ構造
  - 設定タブで CRUD（ドラッグ並び替え対応）
  - 記録タブにポイント残高入力グリッド（前月プレースホルダー・前月コピー対応）
  - `calcTotals()` でポイント合計を現金合計に加算
  - ダッシュボード現金カードの bank-mini にポイント行表示
  - スナップショットに `pointValues` 保存・読み込み
- [x] PDF レポート生成（印刷 CSS 方式）
  - `@media print` CSS（ナビ・ボタン・記録/設定タブ非表示）
  - `#print-header`（通常時非表示、印刷時に KPI サマリーを表示）
  - `printReport()` 関数 + 記録タブ sticky バーに「🖨 印刷/PDF」ボタン

---

## 未完了 / 検討中 🔧

### バグ修正

- [x] **[修正済 v84] `importRakuten` に `FileReader.onerror` がない** — `importAll`/`importSettings` は v74 で対応済みだが楽天CSV インポートのみ漏れ。ファイル読み込み失敗が無音で失敗する
- [x] **[修正済 v84] `chartDrawdown` が `_chartRender()` を使っていない** — renderDrawdown を改修: canvas 永続化・インプレース更新対応・クリア時の確実なチャート破棄
- [x] **[修正済 v84] クイックナビ `simIds` に `sec-reinvest`・`sec-div-sim` が漏れていた** — 再投資シミュ・配当シミュセクションへスクロール時にシミュボタンがハイライトされない

### UI/UX 大規模改善（v104）

- [x] **URLハッシュ同期** — タブ・サブタブを `#record/holdings` 形式でURLに反映。リロード復元・ブラウザバック対応・モーダルを戻るボタンで閉じる
- [x] **記録タブ入力フロー刷新** — 入力進捗インジケーター（未入力ジャンプ付き）・スマホ用保存FAB・金額スマート入力（カンマ/「万」/`+`増分/`-`減算＋円換算ヒント）
- [x] **シミュタブのセグメント切替** — 8セクション縦積みを廃止し、qnav-pill で選んだ1セクションのみ表示（遅延レンダリング）
- [x] **概要タブ ワイド2カラム化**（1100px以上、`.ov-cols`/`.ov-col`）＋ ヒーローにスパークライン（直近12ヶ月）
- [x] **モバイルカード型テーブル**（`.mtbl` — 記録タブ銘柄/iDeCo・分析タブ銘柄別一覧）
- [x] **並び替え ▲▼ ボタン**（設定タブ全テーブル、モバイルのドラッグ&ドロップ代替）
- [x] **チャート強化** — YTD期間ボタン・メモ付き月のマーカー＋ツールチップ表示・SCHD元本/目標ライン（凡例トグル）
- [x] **コマンドパレット**（Ctrl+K — セクション・銘柄・操作の横断検索）
- [x] **キーボードショートカット**（1〜5タブ切替・?ヘルプ）
- [x] **入力保護** — number入力のホイール誤操作防止・設定パネルのオートフォーカス＆フォーカストラップ

### 改善提案

- [x] **[解決 v104] `sec-reinvest` / `sec-div-sim` をクイックナビに追加** — シミュタブのセグメント切替化により全8セクションが pill として表示されるようになった
- [x] **[確認済 v103] スポット ID 生成を `uid()` に変更** — 実コード（`addSpotRow` / `getSpotListFromPanel`）は既に `uid()` を使用していた。`'sp'+Date.now()+i` が残るのは `load()` 内マイグレーションのみで、ここは uid() 禁止制約（過去のデータ全消去バグ再発防止）により意図的に維持
- [x] **[修正済 v103] NISA 超過表示で入力値が負の場合「超過 ¥-xxx」と表示される問題** — 入力欄に `min="0"` 追加＋`saveSnapshot()`/`renderNisaBar()` で負数を0にクランプ

### 機能検討

- [x] **[完了 v100] カード・セクション間隔の全体コンパクト化** — main padding / グリッド gap / `.card` / `.hero` / `.an-block` / `.an-summary` / `.an-body` / `.an-divider` / `.rec-sec` / `.rec-sec-body` のパディング・マージン・border-radius を均一縮小（モバイルオーバーライドも調整）
- [x] **[完了 v103] プライバシーモード** — 目アイコン（サイドバー／モバイルヘッダー）で全金額を `¥•••••` 表示に切替。`localStorage 'asset-privacy'` に保存（エクスポート JSON には含めない）。チャートY軸・ツールチップもマスク、AI分析用エクスポートは実値出力
- [x] **[完了 v101] ページ分割** — ダッシュボードを「概要」「分析」「シミュ」の3ページに分割。ボトムナビ・サイドバーナビを5ボタンに拡張。`renderDashboard()`を`renderOverview()`/`renderAnalysis()`/`renderSimulations()`の3関数に分割。
- [x] **[完了 v103] 損益推移グラフ** — 分析タブ `sec-gainloss` に含み損益＋損益率（右軸）の折れ線チャート。期間ボタンは資産推移と共通（`trendPeriod` 共有）
- [ ] API 連携（楽天証券・楽天銀行）※将来検討
- [x] **[完了 v103] 削除の Undo** — 銘柄・銀行・カード・ポイント・iDeCo・スナップの削除後10秒間、トーストの「元に戻す」で復元（元の位置に splice 復元）。Ctrl+Z の汎用 Undo スタックは未実装
- [ ] Undo を証券会社・口座種別・銘柄種別の削除にも拡大（order 配列の復元が必要なため見送り）
- [ ] ドラッグ&ドロップに挿入位置インジケータ（drop target の細線表示）
- [x] `_chartRender` の適用範囲拡大（v84で drawdown を対応、全チャート最適化完了）
- [x] **[完了 v103] アイコンボタンへの `aria-label` 追加** — ▾フィルター（静的18個＋tax-table動的6個）・バージョンバッジ・モバイルテーマ切替・姉妹アプリリンク・🎲ランダム色・プライバシー切替
- [x] **[完了 v103] バックアップリマインダー** — 最終 `exportAll()` から30日超（または未実施）で概要タブにバナー表示。`localStorage 'asset-last-export'` に記録、「閉じる」はセッション中のみ有効
- [x] **[完了 v103] localStorage 容量警告** — `persist()` で保存データが約5MBの80%（4,194,304文字）超過時に warn トースト（セッション1回）
- [ ] バックアップリマインダーの閾値（30日）を設定で変更できるようにする
- [ ] コマンドパレットの検索対象を拡大（設定項目・ヘルプ項目・スナップショット月など）
- [ ] モーダルをUIボタンで閉じた際に `_modalHist()` で積んだ履歴エントリが残る（次の戻る操作が1回空振りする）— `history.back()` 連動の検討
- [ ] 金額スマート入力の「+/-増分」をモバイル数字キーボードでも使えるように（インクリメントボタン等の代替UI）
