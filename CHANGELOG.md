# CHANGELOG

バージョンごとの変更履歴。バージョン番号は `APP_VERSION`（app.js）・キャッシュバスター（index.html）・`CACHE`（sw.js）の3箇所と同期すること。

---

## v102（2026-06-10）

### スマホ表示の2列グリッド対応
- **`.g3`・`.g4` グリッドをスマホ（768px以下）で2列表示に変更** — サマリーカード・詳細分析統計カード・銀行/カード/ポイント入力グリッドが2列になり、縦スクロール量を削減。

---

## v101（2026-06-09）

### ダッシュボード3ページ分割（概要 / 分析 / シミュ）
- **ダッシュボードを「概要」「分析」「シミュ」の3ページに分割** — 14セクション縦一列の単一ページを、クイックナビの区切り（|）に沿って3つのタブに分割。各ページ間の移動はボトムナビ・サイドバーナビから行う。
- **ボトムナビ・サイドバーナビを5ボタンに拡張** — 概要（house）/ 分析（bar-chart）/ シミュ（activity）/ 記録 / 設定。新規アイコンは Feather Icons 準拠の SVG アウトラインスタイルで統一。
- **概要ページ**: ヒーローカード + SCHD ストリップ + NISA + ポートフォリオ（4セクション）
- **分析ページ**: 資産推移チャート + 全銘柄詳細分析（2セクション）
- **シミュレーションページ**: 資産シミュ〜税金概算（8セクション、フラットqnavで全セクションへアクセス可能）
- **qnavをtab-content内に移動** — CSS sibling selector方式からtab-content子要素方式に変更。各ページのqnavがページ切替で自動表示/非表示される。
- **`renderDashboard()` を3関数に分割** — `renderOverview()` / `renderAnalysis()` / `renderSimulations()`（既存 `renderDashboard()` はラッパーとして存続）。

---

## v100（2026-06-09）

### コンパクト化（カード・セクション間隔の均一縮小）
- **カード・セクション間隔を全体的に縮小** — `main` の padding / `.mb` / グリッド gap をそれぞれ削減。`.card` / `.hero` / `.an-block` / `.sum-card` の border-radius・padding・margin を均一に縮小し、画面をより有効活用できるよう調整。
- **`an-summary` / `an-body` / `an-divider` のパディングを縮小** — セクションヘッダーと本文の余白をコンパクト化。
- **`rec-sec` / `rec-sec-body` の余白を縮小** — 記録・設定タブのカード間隔を詰めてセクション数が多い画面でもスクロール量を削減。
- **モバイルの `.hero` / `.an-summary` / `.an-body` パディングも縮小** — スマホ表示でのコンテンツ密度を向上。

---

## v99（2026-06-09）

### UI統一（姉妹アプリ「給与明細ダッシュボード」とのイラストテイスト統一）
- **ナビアイコンをアウトラインSVG方式に変更** — ボトムナビ（📊📝⚙️）・サイドバーナビ（テキストのみ）・モバイルヘッダーの絵文字を Feather Icons 風のアウトライン系 SVG アイコンに置き換え。`stroke="currentColor"` を使用しているため `.active{color:var(--primary)}` で自動ハイライト。
- **サイドバーナビにアイコンを追加** — 従来テキストのみだったサイドバーのタブボタンに house / file-text / settings アイコンを追加（アイコン＋テキストの flex 配置）。
- **テーマ切替アイコンを CSS-only 切替に変更** — `.tt-ico` 内に moon/sun の 2つの SVG を内包し、`.dark-mode` クラスの有無で表示を切り替え。JS 側の `textContent` 操作を廃止しシンプル化。
- **姉妹アプリリンクの「↗」テキストを external-link SVG アイコンに変更** — サイドバー・モバイルヘッダー両方。

---

## v98（2026-06-07）

### UI統一（姉妹アプリ「給与明細ダッシュボード」とのレイアウト規約共通化）
- **PCレイアウトをサイドバー方式に刷新** — ヘッダー＋上部水平タブナビを廃止し、左固定サイドバー（幅224px）にタイトル・バージョンバッジ・タブナビ・本日の日付・最終更新時刻・ダーク/ライト切替・姉妹アプリへのリンクを集約。
- **スマホレイアウトをモバイルヘッダー＋ボトムナビ方式に刷新** — 上部に簡易ヘッダー（タイトル・バージョンバッジ・テーマ切替・姉妹アプリリンク）、下部に固定ボトムナビ（ダッシュボード／記録／設定）を配置。クイックナビはボトムナビの上に表示する現状の形を維持。
- **CSS変数 `--sidebar-w` / `--topbar-h` / `--bottom-nav-h` を導入** — sticky要素のオフセットやレイアウト幅をブレークポイントに応じて一元管理。
- **`updateTodayDate()` / `updateTs()` / `applyTheme()` / バージョンバッジ初期化処理を `querySelectorAll` ベースに変更** — サイドバーとモバイルヘッダーの両方に同じクラスを持つ要素が存在するため、`id` ではなく共通クラス（`.today-date` / `.last-updated` / `.theme-toggle` / `.app-version-badge`）を介して同期更新するよう修正。
- **`_doSwitchTab()` のタブボタンハイライト処理を `data-tab` 属性ベースに変更** — サイドバーナビとボトムナビの2系統のボタンを同時にハイライトできるよう修正（旧 `.main-nav` の位置インデックス方式から脱却）。

---

## v97（2026-06-07）

### UI統一（姉妹アプリ「給与明細ダッシュボード」とのデザイン規約共通化）
- **分析セクションのカードタイトルに左アクセントバーを追加** — `.an-summary` のスタイルを `border-left` ＋グラデーション背景から、丸型アクセントバー（`width:4px;height:16px;border-radius:99px`）＋テキストの flex レイアウトに変更。CSS のみの変更で全セクション（資産推移・詳細分析・各種シミュレーション等）に反映。
- **チャートY軸ラベルの「万」表記を統一** — `(v/10000).toFixed(0)+'万円'` → `'¥'+(v/10000).toFixed(1)+'万'` に変更（資産推移・iDeCoシミュ・取崩しシミュの3チャート）。配当カレンダーは小額表示の特殊ロジックがあるため変更対象外。
- **タブ切替時にページ最上部へスクロール** — `_doSwitchTab()` に `window.scrollTo(0,0)` を追加。

---

## v96（2026-05-30）

### バグ修正・改善
- **AI分析用エクスポート — iDeCo配分比率の表示修正** — iDeCoの`monthlyAmount`は配分比率（%）なのに¥形式で表示していたのを修正。列名も「月次積立」→「配分比率」に変更。
- **AI分析用エクスポート — NISA枠使用状況の修正** — `D.current.nisa`（手入力値、常に0）ではなく保有銘柄データからダッシュボードと同じ計算方式で表示するよう修正。
- **AI分析用エクスポート — 月次積立サマリーセクション削除** — 不要なセクションを削除し、積立合計は設定情報欄に統合。
- **基本設定に資産形成方針メモ欄を追加** — 自由記述のメモを入力でき、AI分析用エクスポートの冒頭に反映される。

---

## v95（2026-05-30）

### 新機能
- **AI分析用エクスポート** — 設定タブ → データ管理から現在の資産状況を Markdown ファイル（`.md`）としてエクスポート。ChatGPT や Claude に貼り付けてそのまま分析依頼できる。口座残高・保有銘柄（含み損益・月次積立・配当利回り）・iDeCo・NISA枠・月次積立サマリーを含む。

---

## v94（2026-05-30）

### バグ修正
- **楽天証券CSV — 再インポート時の重複作成バグを修正** — 未知の種別・口座種別を再インポートするたびに新しいUIDを生成してマッチ失敗→重複追加していた。修正後は `D.customAssetTypes` / `D.customAccounts` に同ラベルが存在すれば既存IDを再利用する。
- **楽天証券CSV — 確認ダイアログのUSD表示を修正** — USD銘柄の更新確認で円建て値を表示していたが、実際にはUSD値を保存するため不一致だった。ダイアログもUSD単位で表示するよう修正。

---

## v93（2026-05-30）

### バグ修正・改善
- **楽天証券CSV — USD銘柄の二重換算バグを修正** — 米国株式の値を USD 単位（株数×現在値）で保存し、新規追加時は `currency:'usd'` を自動セット。既存の `currency:'usd'` 銘柄を更新する際も USD 値で保存し `holdingJpy()` の二重換算を防止。
- **楽天証券CSV — `toDelete` スコープバグを修正** — 楽天証券ブローカーが未登録（`rakutenBrokerId=''`）のとき、brokerId 未設定の全銘柄が削除対象になる問題を修正。登録済みの場合のみ削除スコープを適用。

---

## v92（2026-05-30）

### 削除
- **楽天証券CSVインポート — JPY預り金サマリー取込み処理を削除** — マネーブリッジ利用時にほぼ常に0となるサマリー行「預り金」の bankAccount 更新処理（yokinBalance）を削除。外貨預り金は保有商品詳細の holding として引き続き取込まれる。

---

## v91（2026-05-30）

### 修正
- **楽天証券CSVインポート — 口座「-」をCSV表記のまま自動作成** — 口座欄が「-」の行（外貨預り金等）を特定口座へ強制マッピングしていた special case を廃止。「-」のまま unknownAcc に入り、CSVの表記と同じラベルで口座種別を自動新規作成するよう修正。

---

## v90（2026-05-30）

### 機能改善
- **楽天証券CSVインポート — 未登録の種別・口座を自動新規作成** — 未知の銘柄種別・口座種別をインポート時に `D.customAssetTypes` / `D.customAccounts` へ自動追加。確認ダイアログに作成される種別名を表示し、設定タブから後から編集可能。

---

## v89（2026-05-30）

### 機能改善
- **楽天証券CSVインポート — 保有商品詳細の全行を取込み** — `_RAKUTEN_TYPE_MAP` にない種別（外貨預り金など）をスキップしていたフィルターを廃止。未知の種別は `'other'`、未知の口座種別は `'specific'` にフォールバックして全行を処理・新規作成。

---

## v88（2026-05-30）

### 機能改善・バグ修正
- **楽天証券CSVインポート — 預り金も取込み** — 「預り金」行を解析し、「預り金」を含む銀行口座の残高を自動更新。確認ダイアログに更新内容を表示。
- **楽天証券CSVインポート — 楽天銀行ラベル修正** — 実際のCSVラベルは「楽天銀行普通預金残高」だったため検索対象を修正（旧「マネーブリッジ残高」との両対応で互換性維持）。

---

## v87（2026-05-30）

### 機能改善
- **楽天証券CSVインポート — マネーブリッジ残高も取込み** — 「マネーブリッジ残高」行を解析し、「楽天」を含む銀行口座の残高を自動更新するよう `_parseRakutenRows` / `_applyRakutenRows` を拡張。確認ダイアログにも残高更新内容を表示。

---

## v86（2026-05-27）

### 新機能
- **楽天SCHDスポット買いおすすめ表示** — SCHDストリップに決算日（2/5/8/11月25日）をもとにした買い時アドバイスを追加。口座種別（NISA/特定口座）を自動判定し、権利落ち後・決算前それぞれに適したメッセージを色分け表示。

---

## v85（2026-05-26）

### 修正
- **ヘルプモーダル更新履歴・使い方を最新化** — v76〜v84 の変更内容をアプリ内ヘルプ（更新履歴タブ・使い方タブ）に反映
- **Service Worker キャッシュ更新** — v84 のキャッシュバスター未更新により古い index.html が PWA に配信され続けていた問題を修正（CACHE を v85 に更新し強制リフレッシュ）
- **ドキュメント整備** — CLAUDE.md / README.md / TODO.md / CHANGELOG.md を v75〜v84 の実装内容に合わせ更新（未ドキュメント関数 30+ 件・CSS クラス追記・データ構造補完）
- **git pre-commit フック強化** — CHANGELOG.md 更新時にヘルプモーダル（index.html の `#help-pane-changelog`）も同時更新しないとコミットをブロックするチェックを追加
- **Claude Code PostToolUse フック追加** — app.js / style.css / index.html 編集後にドキュメント更新チェックリストを自動表示

---

## v84（2026-05-26）

### 修正
- **`importRakuten` に `FileReader.onerror` を追加** — 楽天証券CSVのファイル読み込みエラーが無音で失敗していた問題を修正（`importAll`/`importSettings` は v74 で対応済み、今回で全インポート関数に統一）
- **`renderDrawdown` を `_chartRender` 対応に改修** — canvas を永続化して再描画を最適化。シミュレーション実行ごとに DOM 再生成していたのを、同一パラメータでは `chart.update('active')` によるインプレース更新に変更。クリア時にチャートを確実に破棄するよう修正
- **クイックナビ `simIds` に `sec-reinvest` / `sec-div-sim` を追加** — 再投資シミュレーション・配当シミュレーションセクションへスクロール時にナビのシミュグループボタンがハイライトされていなかった問題を修正

---

## v83（2026-05-26）

### 修正
- **分析レポート NISA 成長枠計算バグ修正** — NISA 成長投資枠の金額が正しく集計されていなかった問題を修正

---

## v82（2026-05-26）

### 追加
- **資産分析レポート機能** — 🔍 ボタンでモーダル表示
  - 口座別・銘柄種別バー比較（実額・比率付き）
  - インサイト自動診断（12条件: 集中リスク・NISA活用状況・iDeCo進捗・配当バランス等）
  - `renderAssetReport()` / `openAssetReport()` / `closeAssetReport()` 関数

---

## v81（2026-05-23）

### 追加・改善
- **現金の色を設定可能に** — 種別合計ドーナツの「現金」色を設定タブで変更できるように
- **凡例クリック ↔ テーブル行シンクロ** — ドーナツグラフの凡例をクリックすると詳細テーブルの対応行をハイライト

---

## v80（2026-05-23）

### 追加
- **種別合計ドーナツに「現金」を追加** — 銀行残高（現金）をポートフォリオの種別合計グラフに含める

---

## v79（2026-05-23）

### 修正
- **スマホヘッダーレイアウト修正** — 480px 以下で「最終更新」テキストを非表示・h1 フォントサイズを縮小してレイアウト崩れを解消

---

## v78（2026-05-23）

### 追加
- **ヘッダーに今日の日付（曜日）を表示** — `updateTodayDate()` でページ読み込み時に当日日付を `#today-date` にセット

---

## v77（2026-05-23）

### 変更
- **月末リマインダーの記録済み判定を廃止** — 月初にスナップ保存済みでも月末通知を送る（毎月の振り返りを促す）

---

## v76（2026-05-23）

### 追加
- **月末リマインダー通知** — Android Chrome PWA で Periodic Background Sync を使って月末に通知
  - `requestNotifPermission()` で通知権限リクエスト
  - `_registerPeriodicSync()` で Service Worker に定期同期を登録
  - `_renderNotifStatus()` でヘッダーの 🔔/🔕 ステータスを更新

---

## v75（2026-05-23）

### 追加
- **ヘルプ・更新履歴モーダル** — バージョンバッジ（`v75`）クリックで開く
  - 「使い方」タブ: 初期設定・毎月の使い方・ダッシュボードの見方・便利な機能を初心者向けに解説
  - 「更新履歴」タブ: バージョンごとの変更内容（v40台〜最新）
  - 背景クリック・ESC キーで閉じる

---

## v74（2026-05-23）

### 改善
- **トースト通知** — タイムアウト 3.2秒 → 5秒、× 閉じるボタン追加（`pointer-events` 修正含む）
- **h1 リロード確認** — 未保存データがある場合、`customConfirm()` でリロードをブロック（`handleTitleClick()`）
- **税率定数一元化** — `TAX_RATE = 0.20315` をファイル先頭で定義、コード内6箇所の直書き数値を置換
- **`formatMonth(d)` ユーティリティ追加** — `Date → 'YYYY-MM'` フォーマットを一元化
- **`FileReader.onerror` 追加** — `importAll` / `importSettings` でファイル読み込みエラーをトーストで通知

---

## v73 以前

v73 以前の変更履歴は `git log` を参照。主な大型リリースは以下の通り。

| バージョン | 主な変更 |
|-----------|---------|
| v73 | スナップショット比較モーダル |
| v72 | 直前バージョンのバグ修正 |
| v71 | 月次サマリーモーダル改善 |
| v67 | PWA 化 / ポイント口座管理 / PDF レポート |
| v60 台 | iDeCo シミュレーション・FIRE 取崩しシミュ・税金概算 |
| v50 台 | 楽天証券 CSV インポート・証券会社 CRUD |
| v40 台 | カスタム口座種別・銘柄種別 CRUD |
| v30 台 | 配当カレンダー・配当シミュ・iDeCo 受取シミュ |
| v20 台 | Excel 風フィルター・目標配分バー・クイックナビ |
| v10 台 | ポートフォリオドーナツ・再投資シミュ・FIRE シミュ |
| v1〜9 | 初期実装（基本 CRUD・スナップショット・資産推移） |
